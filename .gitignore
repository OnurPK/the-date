node_modules
.DS_Store
*.log
.env
.vscode
.idea
_legacy/
_backup/
_debug_frames/
# Local config — keys + worker code stay private. JSON preset
# registries (fx/tr) are frontend-public, whitelist them back in.
_config/
!_config/*.json
*.action-backup
*.backup
*.backup-music
