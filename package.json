{
  "name": "the-date",
  "version": "1.0.0",
  "description": "The Date \u2014 visual novel prototype (static site)",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "serve -l tcp://0.0.0.0:${PORT:-3000}",
    "ios:add": "npx cap add ios",
    "ios:open": "npx cap open ios",
    "ios:sync": "npx cap sync ios"
  },
  "dependencies": {
    "serve": "^14.2.3",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
