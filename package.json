{
  "name": "the-date",
  "version": "1.0.0",
  "description": "The Date — visual novel prototype (static site)",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "serve -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
